# Civic Registration Flow — Development Notes

This project uses React/Vite for the web application and Supabase for authentication, database, storage and realtime.

Keep credentials out of source control. Frontend code may contain only the Supabase publishable/anon key; never expose a service-role or secret key.

Use `npm run dev` for local development and `npm run build` for production builds.
